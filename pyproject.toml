[project]
name = "render-database-restarter"
version = "0.1.0"
description = "Render has a bug related to the Free Tier of the databases: Basically this repository contains an automation that seeks to exploit that vulnerability in order to have unlimited databases"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "dotenv>=0.9.9",
    "flask>=3.1.0",
    "selenium>=4.31.0",
    "setuptools>=78.1.0",
    "undetected-chromedriver==3.5.5",
]
